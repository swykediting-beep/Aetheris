function startApp(){document.getElementById('intro').style.display='none';document.getElementById('mainApp').classList.add('show');}
function createConfetti(){for(let i=0;i<30;i++){const c=document.createElement('div');c.style.position='absolute';c.style.width='10px';c.style.height='10px';c.style.background=`hsl(${Math.random()*360},70%,60%)`;c.style.left=Math.random()*90+'%';c.style.top='0%';c.style.borderRadius='50%';c.style.opacity='0.9';c.style.pointerEvents='none';c.style.transition='all 1s linear';document.getElementById('mainApp').appendChild(c);setTimeout(()=>c.remove(),1000);}}
function shortenUrl(){const longUrl=document.getElementById('longUrl').value;if(!longUrl){alert('Please enter a URL!');return;}const shortUrl='https://shorter.ae/'+Math.random().toString(36).substring(2,8);const r=document.getElementById('result');r.innerHTML=`<a href="${shortUrl}" target="_blank">${shortUrl}</a> <button onclick="copyUrl('${shortUrl}')">Copy</button>`;r.classList.add('show');createConfetti();}
function copyUrl(url){navigator.clipboard.writeText(url).then(()=>alert('URL copied!'));}